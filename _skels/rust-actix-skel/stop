#!/usr/bin/env bash
set -euo pipefail

# Stop script for Rust Actix-web project
# Usage: ./stop

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

echo "Stopping services..."

# Stop Docker container if running
if docker ps -q -f name=actix-app | grep -q .; then
    echo "Stopping Docker container..."
    docker stop actix-app
fi

echo "Done"
