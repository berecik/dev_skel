#!/usr/bin/env bash
set -euo pipefail

SKEL_DIR="$(cd "$(dirname "$0")" && pwd)"
TARGET="${1:-}"

if [[ -z "$TARGET" ]]; then
  echo "Usage: $(basename "$0") <target-path>"
  exit 1
fi

make -C "$SKEL_DIR" gen NAME="$TARGET"
