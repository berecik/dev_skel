#!/usr/bin/env bash
set -euo pipefail

# Stop FastAPI DDD Docker container
# Usage: ./stop

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

echo "Stopping Docker container..."

docker stop fastapi-ddd-app 2>/dev/null || echo "Container not running"

echo "Done"
