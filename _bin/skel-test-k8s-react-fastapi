#!/usr/bin/env python3
"""K8s cross-stack test: FastAPI backend deployed to k3s on paul.

See `_bin/_k8s_lib.py` for the full driver. Generates a wrapper,
builds + pushes the backend image, deploys via Helm, and runs the
canonical items + orders HTTP exercise against the NodePort.

Usage::

    _bin/skel-test-k8s-react-fastapi              # full run
    _bin/skel-test-k8s-react-fastapi --keep       # leave k8s resources deployed
    _bin/skel-test-k8s-react-fastapi --no-skip    # fail on missing toolchain

Exit codes: 0 pass / 1 fail / 2 setup error.
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from _k8s_lib import run_k8s_test  # noqa: E402

if __name__ == "__main__":
    sys.exit(run_k8s_test("python-fastapi-skel"))
